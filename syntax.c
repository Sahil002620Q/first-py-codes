// syntax for if
#include <stdio.h>
int main()
{
    //syntax for if 
    if (condition)
    {
        //code
    }

    //syntax for if else
    if (condition)
    {
        //code
    }
    else
    {
        //code
    }

    //syntax for do 
    do(condition)
        { 
             //code
        }while(condition)
    
    //syntax for else if
    if (condition)
    {
        //code
    }
    else if (condition)
    {
        //code
    }
    else
    {
        //code
    }
    
    //syntax for switch
    switch (condition)
    {
        case 1:
        //code
        break;

        case '*':
        //code
        break;
    }

    //else if syntax
    if (condition)
    {
        //code
    }else if (condition){
        //code
    }else
    {
        //code
    }

    //

//comment

}
